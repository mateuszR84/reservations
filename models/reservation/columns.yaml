# ===================================
#  List Column Definitions
# ===================================

columns:
    client_name:
        label: mater.reservations::lang.models.reservation.client
        relation: client
        select: first_name
    service_name:
        label: mater.reservations::lang.models.reservation.service
        relation: service
        select: service_name
    date:
        label: mater.reservations::lang.models.reservation.date
        type: date
    hour:
        label: mater.reservations::lang.models.reservation.start
    revoke_reservation:
        label: mater.reservations::lang.models.reservation.revoke
        type: partial
